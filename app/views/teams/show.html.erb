<h1>Team: <%=@team.name %></h1>

Team Members:
<ul>
  <% @team.users.each do |user| %>
    <li><%=link_to user.name, user_url(user) %></li>
    <% end %>
  </ul>

Projects List:
<ul>
  <% @team.projects.each do |project| %>
    <li><%=link_to project.title, team_project_url(project) %></li>
    <% end %>
  </ul>

  <%= link_to "Edit this Team", edit_team_url(@team.id)%><br/ ><br/ >

  <%= link_to "Make a New Project for this Team", new_team_project_url(@team.id)%> <br/ ><br/ >

  <%= link_to "Back to Team List", teams_url%>