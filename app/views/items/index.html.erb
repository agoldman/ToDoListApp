<h1>Items List</h1>
<ul>
  <% @items.each do |item| %>
  <li><%= link_to item.title, item_url(item) %></li>
  <% end %>
</ul>
<%= link_to "Create A New Item", new_item_url %><br />

<div>
  <%= link_to "All Projects To Do", projects_url %> <br />
<div>